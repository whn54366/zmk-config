name: Build ZMK Firmware

on:
  push:
  workflow_dispatch:

jobs:
  build:
    uses: zmkfirmware/zmk/.github/workflows/build-user-config.yml@v0.3
    with:
      matrix:
        include:
          - board: nice_nano_v2
            shield: wwn_fight_keyboard